Test

<%= simple_form_for @collaterals_tag, url: collateral_tags_path do |c| %>
  <%= c.input :weight %>
  <%= c.select :tag_id, @tags.map { |r| [r.name, r.id] }, include_blank: false %>
  <%= c.button :submit %>
<% end %>