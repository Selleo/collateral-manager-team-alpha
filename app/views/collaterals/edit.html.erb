<h1>Test teste test</h1>
<%= simple_form_for @collateral do |c| %>
  <%= c.input :title %>
  <%= c.input :link %>
  <%= c.input :content_type %>
  <%= c.button :submit %>
<% end %>

<% @collateral.tags.order(:category, :name).each do |tag| %>
  <span class="badge bg-success"><%= tag.category %>: <%= tag.name %> </span>
<% end %>
